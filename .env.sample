VITE_FULL_VERSION_INFO=dev
VITE_APP_ENV=dev

# Backend URL
# 옵션 A — 단순 localhost (proxy 미사용)
VITE_VAULTKEEPER_BACKEND_URL=http://localhost:3000/api/v1

# 옵션 B — local-vk 프록시 (dev/prod parity, mkcert + /etc/hosts 셋업 필요)
# VITE_VAULTKEEPER_BACKEND_URL=https://local-vk.doctorkeeper.com/api/v1
